# Dépendances minimales pour streamlit_dashboard_local.py + le téléchargement des données.
# Volontairement séparées de pyproject.toml qui contient aussi dbt-core, dbt-snowflake,
# jupyter, matplotlib... inutiles ici et lourds sur un VPS à l'espace disque limité.
streamlit>=1.50.0
duckdb>=1.5.1
pandas>=2.3.3
plotly>=6.3.1
pyarrow>=21.0.0
httpx>=0.28.1
loguru>=0.7.3
